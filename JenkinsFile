pipeline {
    agent any
 
    environment {
        withAWS(region: 'eu-north-1', credentials: 'my-aws-credentials')
    }
 
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/harish44442/POC4_terraform_validation.git'
            }
        }
 
        stage('Terraform Init') {
            steps {
                sh 'terraform init'
            }
        }
 
        stage('Terraform Validate') {
            steps {
                sh 'terraform validate'
            }
        }
 
        stage('Terraform Plan') {
            steps {
                sh 'terraform plan -out=tfplan'
            }
        }
 
        stage('Terraform Apply (Optional)') {
            when { expression { return false } } // set to true to auto-apply
            steps {
                sh 'terraform apply -auto-approve tfplan'
            }
        }
    }
 
    post {
        always {
            echo 'Cleaning up workspace...'
            cleanWs()
        }
        failure {
            mail to: 'harisharun42@gmail.com', subject: 'Terraform Jenkins Pipeline Failed'
        }
    }
}
